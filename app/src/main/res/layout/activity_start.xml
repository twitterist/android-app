<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="org.twitterist.app.home.HomeActivity"
    android:background="#ffffff">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Welcome by Twitterist"
        android:layout_marginTop="96dp"
        android:id="@+id/homeActivity"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true" />

    <!-- Twetter Button-->
    <ImageButton
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/imageButton_Twitter_Icon"
        android:src="@mipmap/ic_twitter_icon_blue"
        android:background="#ffffff"
        android:scaleType="fitXY"
        android:layout_alignTop="@+id/imageButton_Analysis_Icon"
        android:layout_toLeftOf="@+id/homeActivity"
        android:layout_toStartOf="@+id/homeActivity" />

    <!-- Analysis Button-->
    <ImageButton
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/imageButton_Analysis_Icon"
        android:src="@mipmap/ic_serach_icon"
        android:background="#ffffff"
        android:layout_marginBottom="48dp"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true" />

    <!-- AboutUs Search Button-->
    <ImageButton
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/imageButton_AboutUs_Search_Icon"
        android:src="@mipmap/ic_search_icon"
        android:background="#ffffff"
        android:layout_alignTop="@+id/imageButton_Analysis_Icon"
        android:layout_toRightOf="@+id/homeActivity"
        android:layout_toEndOf="@+id/homeActivity" />

</RelativeLayout>
